<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WinUIDemoApp.MyControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WinUIDemoApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <TextBlock Text="{x:Bind MyControlNumber, Mode=OneWay}"/>
        <!--<TextBox
            FontSize="64"
            Text="{x:Bind MyControlNumber, Mode=OneWay}" />
        <TextBox
            FontSize="64"
            Text="{x:Bind MyControlNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <TextBox Text="{x:Bind Test, Mode=OneWay}" />
        <TextBox Text="{x:Bind Test, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />-->
    </StackPanel>
</UserControl>
