<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinUIDemoApp.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WinUIDemoApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid Margin="8" ColumnDefinitions="*,*,*">
        <StackPanel Grid.Column="0" Spacing="4">
            <Button Command="{x:Bind ViewModel.AddItemCommand}" Content="Add item" />
            <Button
                Command="{x:Bind ViewModel.RemoveItemCommand}"
                CommandParameter="{x:Bind ItemsListView.SelectedIndex, Mode=OneWay}"
                Content="Remove item" />
            <Button
                Command="{x:Bind ViewModel.ReplaceItemCommand}"
                CommandParameter="{x:Bind ItemsListView.SelectedIndex, Mode=OneWay}"
                Content="Replace item" />
            <Button Command="{x:Bind ViewModel.MoveItemCommand}" Content="Move item" />
            <Button Command="{x:Bind ViewModel.ResetItemsCommand}" Content="Reset items" />
        </StackPanel>
        <ListView
            x:Name="ItemsListView"
            Grid.Column="1"
            ItemsSource="{x:Bind ViewModel.Items}"
            SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <ListView
            x:Name="LogsListView"
            Grid.Column="2"
            ItemsSource="{x:Bind ViewModel.Logs}"
            SelectedItem="{x:Bind ViewModel.SelectedLog, Mode=OneWay}" />
    </Grid>

</Page>
