<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinUIDemoApp.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WinUIDemoApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Page.Resources>
        <local:TreeViewItemTemplateSelector x:Key="TreeViewItemTemplateSelector">
            <local:TreeViewItemTemplateSelector.DataTemplates>

                <DataTemplate x:Key="LeafNode" x:DataType="local:LeafNode">
                    <TreeViewItem Content="{x:Bind Content}" />
                </DataTemplate>

                <DataTemplate x:Key="BranchNode" x:DataType="local:BranchNode">
                    <TreeViewItem
                        Content="{x:Bind Content}"
                        FontWeight="ExtraBold"
                        ItemsSource="{x:Bind Children}" />
                </DataTemplate>

            </local:TreeViewItemTemplateSelector.DataTemplates>
        </local:TreeViewItemTemplateSelector>
    </Page.Resources>

    <TreeView ItemTemplateSelector="{StaticResource TreeViewItemTemplateSelector}" ItemsSource="{x:Bind ViewModel.Nodes}" />

</Page>
