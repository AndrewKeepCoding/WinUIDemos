<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinUIDemoApp.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    xmlns:local="using:WinUIDemoApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <labs:CanvasView ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}">
        <labs:CanvasView.ItemTemplate>
            <DataTemplate x:DataType="local:Item">
                <Button
                    Canvas.Left="{x:Bind X, Mode=TwoWay}"
                    Canvas.Top="{x:Bind Y, Mode=TwoWay}"
                    ManipulationMode="TranslateX,TranslateY">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Content}" />
                        <TextBlock>
                            <Run Text="{x:Bind X, Mode=OneWay}" />
                            <Run Text="," />
                            <Run Text="{x:Bind Y, Mode=OneWay}" />
                        </TextBlock>
                    </StackPanel>
                </Button>
            </DataTemplate>
        </labs:CanvasView.ItemTemplate>
    </labs:CanvasView>

</Page>
